name: Create a yak page and push to azure
on: push

env:
  YAK_PATH: './grasshoperYak'

jobs:
  upload:
    runs-on: windows-latest
    steps:
      - name: 🛎 Checkout repo
        uses: actions/checkout@v2
        with: 
          fetch-depth: 0
          
      - name: Login via Az module
        uses: azure/login@v1
        with:
          creds: ${{secrets.AZURE_CREDS}}
          enable-AzPSSession: true
          
      - name: Deploy Hub
        uses: azure/powershell@v1
        with:
          inlineScript: |
            $ctx = New-AzStorageContext -ConnectionString "DefaultEndpointsProtocol=https;AccountName=sandboxgh;AccountKey=Ge1eymfgWfs4wiU8o5mQ0UcmvDJS9xwQ065HPgm4cv+5q2m94CiJ8QrrHfSKql4CevYm6dP9zQyq+ASt2kNAPw==;EndpointSuffix=core.windows.net"
            echo $ctx
          azPSVersion: "latest"
